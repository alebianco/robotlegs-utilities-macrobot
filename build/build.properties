#Properties file for build.xml
project.name=robotlegs-utilities-macrobot

#Version number for current Robotlegs and Swiftsuspenders releases
version.num=v0.9.7
project.name.versioned=${project.name}-${version.num}

#build locations
main.src.dir=${basedir}/src
test.src.dir=${basedir}/test
doc.dir=${basedir}/docs
lib.dir=${basedir}/libs
bin.dir=${basedir}/bin
dist.dir=${basedir}/release
report.dir=${basedir}/reports